{% extends 'base.html.twig' %}

{% block title 'Contact' %}

{% block body %}
<div class="example-wrapper">
    <h1 class="text-center">Contact form</h1>

    {{ form_start(form, {attr: {class: 'w-50 m-auto'}}) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Send message</button>
    {{ form_end(form) }}
</div>
{% endblock %}
